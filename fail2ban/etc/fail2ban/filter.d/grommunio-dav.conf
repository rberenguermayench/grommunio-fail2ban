# File: /etc/fail2ban/filter.d/grommunio-dav.conf
# Fail2Ban grommunio-dav filter
# (c) 2021 by Walter@Hofstaedtler.com
# Stored in WHIE GIT: git@icc-srv:/whie/grommunio/fail2ban.git

[INCLUDES]
before = common.conf

[Definition]
# Option:  failregex
# Notes.:  regex to match the password failures messages in the logfile. The
#          host must be matched by a group named "host". The tag "<HOST>" can
#          be used for standard IP/hostname matching and is only an alias for
#          (?:::f{4,6}:)?(?P<host>[\w\-.^_]+)
# Values:  TEXT
#
# standard and reverse proxy
failregex = dav.*user.\S*.*IP.(<HOST>)

ignoreregex = .*main Uncaught exception$

#[Init]

#journalmatch = _SYSTEMD_UNIT=dovecot@-.service

# --- eof ---
# vim: syntax=auto ts=4 sw=4 sts=4 sr noet